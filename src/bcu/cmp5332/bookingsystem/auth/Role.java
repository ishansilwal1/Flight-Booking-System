package bcu.cmp5332.bookingsystem.auth;

/**
 * The Role enum represents the different roles a user can have in the flight booking system.
 */
public enum Role {
    /**
     * Represents an administrator with elevated privileges.
     */
    ADMIN,
    
    /**
     * Represents a customer with standard privileges.
     */
    CUSTOMER
}